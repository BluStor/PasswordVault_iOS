//
//  Theme.swift
//  PasswordVault
//

import UIKit

struct Theme {

    struct Base {
        static let navigationItemButtonPulseColor = UIColor(hex: 0xA0E0FF)
        static let navigationBarTintColor = UIColor.white
        static let navigationBarBarTintColor = UIColor(hex: 0x03A9F4)
        static let viewBackgroundColor = UIColor.white
    }

    struct Buttons {
        static let mutedBackgroundColor = UIColor(hex: 0xEAEAEA)
        static let mutedTitleColor = UIColor(hex: 0x999999)
        static let normalBackgroundColor = UIColor(hex: 0x00BCD4)
    }

    struct Group {
        static let floatingActionButtonBackgroundColor = UIColor(hex: 0x00BCD4)
        static let floatingActionMenuItemBackgroundColor = UIColor(hex: 0x455A64)
    }
}
